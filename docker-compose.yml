version: "3"
services:
  api:
    build: .
    ports:
      - "5000:8080"
    environment:
      - APP_ENV=production
    depends_on:
      - db
  db:
    image: postgres:latest
    environment:
      - POSTGRES_PASSWORD=postgres
